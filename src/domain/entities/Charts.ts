export interface MonthlyIncome {
  month: string;
  totalIngresado: number;
}

export interface ExpectedVsPaid {
  month: string;
  esperado: number;
  pagado: number;
}

export interface AgeDistribution {
  rangoEdad: string;
  total: number;
}

