'use-client';
import RegisterForm from "@/components/register/RegisterForm";

export default function RegisterPage() {
    return (
//###################################################
//El <RegisterForm /> carga el componente con el mismo nombre que se encuentra en /componentes/register/RegisterForm.tsx
//Se encarga de mostrar el formulario de registro y manejar la logica del mismo
//###################################################
        <RegisterForm />
    )
}