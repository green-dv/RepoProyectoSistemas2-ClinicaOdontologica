'use client';
import CalendarComponent from "@/components/dates/DatesCalendar";

export default function DatesPage(){
    return(
        <main>
            <CalendarComponent/>
        </main>
    );
}
